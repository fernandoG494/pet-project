import React, {useEffect} from 'react';

interface InputProps {
    asteroids: {};
}

const AsteroidRender = ({asteroids}: InputProps) => {

    return (
        <div>AsteroidRender</div>
    );
};

export default AsteroidRender